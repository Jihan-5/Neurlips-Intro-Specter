# Addendum → AC confidential comment (edit the existing comment or post as follow-up, before Aug 3 AoE)

One update since our summary above: Reviewer Mahh has responded, maintaining their score while stating that "the real user data on PersonalWAB, the matched budget Iter-VRP control, and the ablations showing the closed loop is load bearing meaningfully address my concerns." The reviewer's remaining reservation is that fault-localization accuracy is not yet validated against human labels on natural trajectories — a measurement no existing dataset supplies. We have committed to constructing and releasing exactly that instrument for the camera-ready (two-annotator, κ-adjudicated fault labels on real PersonalWAB and TravelPlanner-native failures), alongside the profile-node candidate-set ablation the reviewer names. We note that the concern now standing between this paper and acceptance is a bounded, named measurement with a committed protocol, not an open-ended validity question — and that on PersonalWAB the ground truth is the real user's own behavior, external to our framework. We respectfully ask that the response-period record — ~140k paired trajectories, wins, losses, and scope boundaries reported together, acknowledged by the most critical reviewer as meaningfully addressing their concerns — be weighed in the final decision.

---
*Draft notes (not posted): ~1,250 chars. Quotes Mahh verbatim (their exact words are the strongest asset you now have). Phase 3 is AC/reviewer-only, so this is the last author-visible input the AC gets.*
